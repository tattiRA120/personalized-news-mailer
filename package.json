{
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250514.0",
    "wrangler": "latest"
  },
  "dependencies": {
    "fast-xml-parser": "^5.2.3",
    "typescript": "^5.8.3",
    "linalg-wasm": "file:./linalg-wasm/pkg"
  },
    "scripts": {
        "build:wasm": "cd linalg-wasm && ~/.cargo/bin/wasm-pack build --target web && cp -r pkg/. ../public/wasm/",
        "deploy": "npm run build:wasm && wrangler deploy"
    }
}
