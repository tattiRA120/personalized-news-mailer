{
  "type": "module",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260626.1",
    "@hono/vite-build": "^1.11.1",
    "@types/node": "^26.0.1",
    "drizzle-kit": "^0.31.10",
    "knip": "^6.21.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.0",
    "wrangler": "^4.105.0"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "fast-xml-parser": "^5.9.3",
    "hono": "^4.12.27",
    "honox": "^0.1.56",
    "linalg-wasm": "^0.1.0"
  },
  "scripts": {
    "build": "vite build --mode client && vite build && cp public/wasm/linalg_wasm_bg.wasm dist/linalg_wasm_bg.wasm",
    "build:wasm": "cd linalg-wasm && wasm-pack build --target web && mkdir -p ../public/wasm && cp -r pkg/. ../public/wasm/",
    "deploy": "npm run build && npm run build:wasm && wrangler deploy",
    "dev": "vite dev",
    "knip": "knip"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}